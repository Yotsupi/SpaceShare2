スペース

<%= month_calendar events: @reservations do |date, reservations| %>
  <%= date.day %>

  <% reservations.each do |reservation| %>
    <div>
      <%= reservation.start_time.strftime('%H:%M') %>
      <%= link_to host_reservation_path(reservation.id) do %>
       <%= reservation.guest.last_name %>
      <% end %>
    </div>
  <% end %>
<% end %>