<div class="container">
  <h2>ゲストログイン</h2>
  <br>
  <div class="row">
    <div class="col-8">
     <h5>会員の方はこちらからログイン</h5>
    </div>
  </div>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field">
      <%= f.label "メールアドレス" %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div>

    <div class="field">
      <%= f.label "パスワード" %><br />
      <%= f.password_field :password, autocomplete: "current-password" %>
    </div>

    <div class="actions">
      <%= f.submit "ログイン", class: "btn btn-sm btn-primary" %>
    </div>
  <% end %>

  <br>

  <div class="row">
    <div class="col-8">
      <h5>会員登録がお済みでない方</h5>
      <p>
        <%= link_to "こちら", "/guests/sign_up" %>から新規登録を行ってください。
      </p>
    </div>
  </div>

</div>
